[project]
name = "developer-documentation"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "properdocs",
    "mkdocs-git-revision-date-plugin",
    "mkdocs-material",
    "mkdocs-minify-plugin",
    "mkdocs-redirects",
]
